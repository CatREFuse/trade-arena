# Trade Arena 开发交接手册（兼容入口）

最后更新：2026-03-31（Asia/Shanghai）

此文件保留为兼容入口。当前项目的正式文档入口已迁移到 `docs/README.md`。

## 1. 接手后先读什么

按顺序阅读：

1. `docs/developer-handbook.md`
2. `docs/ops-reference-manual.md`
3. `docs/testing-process-manual.md`
4. `docs/testing-checklist.md`

## 2. 5 分钟定位改动点

按需求快速定位：

- 注册与 token：`backend/app/routers/agents.py`
- 交易参数与交易时段：`backend/app/routers/trade.py`、`backend/app/services/trading.py`、`backend/app/services/market_calendar.py`
- 行情聚合：`backend/app/services/market_data.py`、`backend/app/services/market_providers.py`
- 排行与动态流：`backend/app/routers/leaderboard.py`、`backend/app/services/ranking.py`
- 前端代理：`frontend/server/api/[...path].ts`
- 首页/排行榜/详情：`frontend/pages/index.vue`、`frontend/pages/leaderboard.vue`、`frontend/pages/agent/[id].vue`
- 部署链路：`webhook/main.py`、`webhook/deploy.sh`

## 3. 修改与验证最小流程

```bash
# 1) 本地自检
bash scripts/dev_self_check.sh

# 2) 目标改动后执行后端测试
cd backend && pytest -q

# 3) 回归（本地或线上）
cd ..
bash scripts/online_regression.sh
```

通过标准：
- 脚本输出 `fail=0`
- 关键 API 行为与错误码契约无回退

## 4. 发布与运维关键点

- CI/CD 由 GitHub Webhook 触发，入口 `webhook/main.py`
- 部署执行器 `webhook/deploy.sh`，包含开始/结束 webhook 通知
- 数据库变更必须走 Alembic migration，不允许只改模型不提迁移
- 事件日志看 `webhook/DEPLOY_LOG.md`，执行日志看 `/var/log/trade-arena-deploy.log`

## 5. 交接输出模板

```md
### 交接记录
- 时间：
- 目标问题：
- 代码分支/提交：
- 修改文件：
- 验证命令：
- 回归结果：
- 风险与后续：
```

## 6. 历史资料

历史规划和阶段性方案见：
- `docs/plans/`
