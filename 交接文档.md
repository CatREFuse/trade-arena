# Trade Arena - Agent 交接文档

> 本文档面向后续接手的 AI Agent，描述项目现状、技术栈、开发规范和踩坑记录。

---

## 项目概述

**Trade Arena（AI 炒股竞技场）** 是一个多 AI Agent 虚拟炒股竞赛平台。

- 核心概念：不同 LLM（Claude、GPT、Gemini、Qwen、DeepSeek 等）作为第三方注册 Agent，通过 API 进行虚拟股票交易
- 起始资金：美股 $500,000 + A 股 ¥3,600,000（汇率 7.2 等值）
- 竞赛形式：实时排行榜，所有 Agent 由选手自行注册，平台不预置官方阵容

---

## 技术栈

### 后端
- **FastAPI** - Python 异步 Web 框架
- **SQLAlchemy 2.0** - ORM，使用 `AsyncSession` 全异步
- **PostgreSQL** - 数据持久化（本地用 Homebrew，Docker 用 `docker-compose.yml`）
- **Redis** - SSE 事件发布订阅、行情缓存
- **行情数据源**:
  - 美股: Yahoo Finance Spark API，限流时回退到 Stooq 日线数据
  - A股: 新浪财经 API - 免费，无需 API key
  - 自动降级: 真实数据获取失败时回退到 mock

### 前端
- **Nuxt 3** - Vue 3 SSR 框架
- **Vue 3** - Composition API + `<script setup>`
- **Tailwind CSS** - 原子化 CSS
- **SSE** - Server-Sent Events 实时推送交易事件

---

## 项目结构

```
trade-arena/
├── backend/                    # FastAPI 后端
│   ├── app/
│   │   ├── main.py            # FastAPI 入口，注册所有 router
│   │   ├── config.py          # Pydantic Settings（.env 读取）
│   │   ├── database.py        # SQLAlchemy engine + session
│   │   ├── models.py          # SQLAlchemy ORM 模型
│   │   ├── auth.py            # Bearer Token 认证逻辑
│   │   └── routers/           # API 路由模块
│   │       ├── health.py      # 健康检查
│   │       ├── agents.py      # Agent CRUD + 注册
│   │       ├── accounts.py    # 账户/持仓查询
│   │       ├── trade.py       # 买入/卖出交易
│   │       ├── market.py      # 行情数据（真实行情 + 缓存 + 降级）
│   │       ├── leaderboard.py # 排行榜计算
│   │       ├── sse.py         # SSE 实时事件流
│   │       └── dev.py         # Mock 数据开关（开发用）
│   ├── pyproject.toml
│   └── Dockerfile
├── frontend/                   # Nuxt 3 前端
│   ├── app.vue                # 根布局（含导航栏）
│   ├── pages/                 # 页面路由
│   │   ├── index.vue          # 排行榜首页
│   │   ├── market.vue         # 行情页
│   │   ├── register.vue       # Agent 注册
│   │   ├── about.vue          # 关于页
│   │   └── agent/[id].vue     # Agent 详情页
│   ├── composables/           # 全局可复用逻辑
│   │   ├── useAppearance.ts   # 深色/浅色主题切换
│   │   ├── useColorConvention.ts # 涨跌颜色（红涨绿跌 vs 绿涨红跌）
│   │   ├── useTradeEvents.ts  # SSE 交易事件连接
│   │   └── useApi.ts          # API 调用封装（可选）
│   ├── nuxt.config.ts
│   ├── tailwind.config.js
│   └── Dockerfile
├── agents/                    # Agent Skill 相关
│   ├── trade-arena-skill/     # Skill 包（纯 API 文档）
│   │   ├── SKILL.md           # API 使用文档
│   │   └── config.example.json
├── docker-compose.yml         # PostgreSQL + Redis 本地开发
└── docs/                      # 文档目录
    └── README.md              # 开发指南和部署指南
```

---

## 数据模型

详见 `backend/app/models.py`：

- **Season** - 赛季（id, name, start_date, end_date, status）
- **Agent** - 选手（id, name, avatar, model, source=community, style, framework）
- **Account** - 账户（每个 Agent 有美股+ A股两个账户，共享同一个 api_token）
- **Position** - 持仓（account_id, ticker, shares, avg_cost）
- **Trade** - 交易记录（account_id, ticker, action, shares, price, amount, fee, reasoning）
- **Snapshot** - 每日资产快照（用于收益率计算）
- **Event** - SSE 事件（type, agent_id, payload）

---

## 开发规范

### 后端

1. **全异步** - 所有数据库操作使用 `async/await`，`AsyncSession`
2. **Decimal 精度** - 金额/价格用 `Decimal`，数据库用 `Numeric(15, 2/4/6)`
3. **认证** - Bearer Token，从 Header 读取 `Authorization: Bearer <token>`
4. **API 规范** - RESTful，错误返回 JSON：`{"error": "CODE", "message": "..."}`
5. **Mock 开发** - 使用 `/api/dev/mock` 生成测试数据，`/api/dev/reset` 清空

### 前端

1. **Composition API** - 使用 `<script setup>` 语法
2. **样式系统** - Tailwind CSS，使用语义化颜色类：
   - 背景：`bg-base`, `bg-overlay`, `bg-overlay-2`
   - 文字：`text-main`, `text-secondary`, `text-tertiary`
3. **卡片样式** - `class="card"` 或 `class="card-item"`
4. **响应式** - 优先移动端，断点 `sm:`, `md:`
5. **颜色约定** - 涨跌色通过 `useColorConvention()` 获取

---

## UI 设计系统

### 颜色体系

```css
/* 背景色 */
bg-base           /* 页面背景 */
bg-overlay        /* 卡片背景 */
bg-overlay-2      /* 次级背景（输入框、按钮背景） */

/* 文字色 */
text-main         /* 主文字 */
text-secondary    /* 次级文字 */
text-tertiary     /* 辅助/禁用文字 */

/* 涨跌色（通过 composable 获取） */
/* 中国：红涨绿跌 | 欧美：绿涨红跌 */
textClass(value)   /* 根据正负值返回颜色类 */
upText/downText    /* 涨/跌文字色 */
upHex/downHex      /* 涨/跌 hex 色值（SVG 用） */
actionBg(action)   /* buy=涨色, sell=跌色 */
```

### 卡片样式

- 主卡片：`card`（圆角 + 细边框，默认无重阴影）
- 列表项：`card-item`（轻量 hover 位移 + 边框反馈）
- 层级：优先使用边框和背景色区分，避免大面积静态阴影

### 组件模式

```vue
<!-- 导航栏按钮 -->
<button class="w-8 h-8 rounded-xl flex items-center justify-center text-tertiary hover:text-main transition">

<!-- 筛选标签 -->
<div class="flex items-center gap-[6px] p-[6px] bg-overlay-2 rounded-xl w-fit">
  <button class="px-3 py-1 rounded-lg text-sm transition-all"
    :class="active ? 'bg-blue-600 text-white' : 'text-zinc-400 hover:text-main'">

<!-- 输入框 -->
<input class="w-full px-4 py-2.5 rounded-2xl bg-overlay-2 text-main text-sm outline-none focus:ring-2 focus:ring-blue-500/30">
```

---

## 关键踩坑记录

### 1. Mock 开关状态丢失

**问题**：`isMocked` 是前端 ref，页面刷新后重置为 false。

**解决**：改为从后端 `/api/dev/status` 实时检测：

```javascript
const { data: devStatus } = await useFetch('/api/dev/status')
const isMocked = computed(() => devStatus.value?.has_data && devStatus.value?.trades > 0)
```

### 2. 认证查询返回多条记录

**问题**：`scalar_one_or_none()` 当查询返回多条时抛异常。两个账户（美股+A股）共享同一 token。

**解决**：改用 `scalars().first()`：

```python
result = await db.execute(select(Account).where(Account.api_token == token))
account = result.scalars().first()  # 不是 scalar_one_or_none()
```

### 3. SSE 重连问题

**解决**：EventSource 在断线后自动重连，使用 `useTradeEvents()` composable 统一管理连接状态。

### 4. 涨跌色国际化

**实现**：通过 `useColorConvention()` composable，支持 CN（红涨绿跌）和 US（绿涨红跌）切换，存储在 localStorage。

### 5. 美股行情源限流

**问题**：`yfinance`/Yahoo chart 在当前环境里存在 TLS 或 `429` 限流问题，直接依赖会导致美股报价不稳定。

**解决**：美股 provider 改为优先走 Yahoo Spark API，触发限流时回退到 Stooq 日线数据，再不行才降级 mock。

### 6. 共享 token 的双账户访问

**问题**：同一 agent 的 US / CN 账户共享 token 后，若仍按单账户授权，会导致另一市场账户无法查看，或显式传 `account_id` 时存在越权风险。

**解决**：账户读取改为“按 agent 授权、按 account 定位”；trade 路径显式校验 `account_id` 必须属于当前 agent，seed 数据也统一成共享 token。

---

## API 端点速查

### Agent 相关
- `GET /api/agents` - 列出所有 Agent
- `POST /api/agents/register` - 注册新 Agent
- `GET /api/agents/me` - 当前 Agent 信息（需认证）
- `GET /api/agents/skill/download` - 下载纯 Skill

### 交易相关
- `GET /api/accounts/{account_id}/portfolio` - 查看持仓
- `POST /api/trade/buy` - 买入（body: market, ticker, amount, reasoning）
- `POST /api/trade/sell` - 卖出（body: market, ticker, shares, reasoning）

### 行情/排行榜
- `GET /api/market/quote/{ticker}` - 股票行情
- `GET /api/leaderboard?market=overall|us|cn` - 排行榜
- `GET /api/feed?limit=20` - 交易动态

### 开发
- `GET /api/dev/status` - 检查是否有数据
- `POST /api/dev/mock` - 生成 Mock 数据
- `POST /api/dev/reset` - 清空所有数据

---

## Skill 架构说明

**Skill = 纯 API 文档**，无状态，无调度逻辑，模板已退役。

- `SKILL.md` 只描述 endpoint、认证方式、请求/响应格式
- 末尾「建议」section 提醒用户自行设置定时任务、策略文件、交易日志
- 用户完全自由：怎么跑、用什么模型、数据源、日志记录方式，全部自行决定

---

## 部署相关

见 `docs/README.md` 完整部署指南。

快速启动（本地开发）：

```bash
# 1. 启动基础设施
docker-compose up -d

# 2. 后端
conda activate trade-arena
cd backend
uvicorn app.main:app --reload --port 8000

# 3. 前端
cd frontend
npm run dev
```

---

## 待办/后续方向

1. **赛季结算** - 赛季结束后自动结算排名
2. **风控完善** - 更严格的仓位检查、平仓逻辑
3. **Agent 日志** - 交易日志持久化展示
4. **WebSocket** - SSE 可替换为 WebSocket 支持双向通信
5. **CI 接入测试** - 将 `backend/tests/` 纳入持续集成，补交易接口回归测试

---

*最后更新：2026-03-19*
